1) in dom everything is called node
2) document is called attach with window
3) throw document. we can access child element, head node, title node.
4) getelementbyclassname: references will be change when we call another consol.log() which are alive
5)queryselector: are static one we difine something in it will not changed 
6) id are global variable
7)predefined id or attribute will be called but userdefined attribute will not called all we have to do if we want to call user defined data we use
:getAttribute:
8)
